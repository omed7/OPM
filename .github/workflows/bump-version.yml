name: Bump Version

on:
  pull_request:
    types: [closed]
    branches:
      - main

concurrency:
  group: opm-main-writers
  cancel-in-progress: false

jobs:
  bump-version:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Refresh main
        run: git pull --ff-only origin main

      - name: Bump version
        id: bump
        run: |
          VERSION_FILE="public/version.json"
          if [ ! -f "$VERSION_FILE" ]; then
            echo '{ "version": "1.0.0" }' > "$VERSION_FILE"
          fi

          CURRENT_VERSION=$(jq -r '.version' "$VERSION_FILE")
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"

          MINOR=$((MINOR + 1))
          PATCH=0

          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"

          jq ".version = \"$NEW_VERSION\"" "$VERSION_FILE" > tmp.json && mv tmp.json "$VERSION_FILE"

          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Commit and sync
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -- public/version.json
          if [ "$(git diff --cached --name-only)" != "public/version.json" ]; then
            echo "::error::Unexpected generated files staged for commit."
            exit 1
          fi
          git commit -m "Bump version to ${{ steps.bump.outputs.new_version }}"
          git push origin HEAD:main
