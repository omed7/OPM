# OPM

Automated football xG and expected goals prediction pipeline.

## Environment Setup
To run the automated pipeline locally, ensure the following environment variables are set:
- `API_FOOTBALL_KEY`: Your API-Football secret key. Required for Besta deild karla data.

```bash
export API_FOOTBALL_KEY="your-secret-key"
python3 src/output_writer.py
```
